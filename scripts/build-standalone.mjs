import { execFileSync } from 'node:child_process';
import { readFileSync, writeFileSync } from 'node:fs';
import { resolve } from 'node:path';
const root = resolve(import.meta.dirname, '..');
execFileSync('node', [resolve(root, 'scripts/build-smoke-bundle.mjs')], { stdio: 'inherit' });
const css = ['tokens.css','layout.css','ui.css'].map((name) => readFileSync(resolve(root,'src/styles',name),'utf8')).join('\n');
const js = `globalThis.__TAPHOA_DEMO__=true;\n${readFileSync(resolve(root,'tests/.smoke-bundle.js'),'utf8')}`;
const html = `<!doctype html><html lang="vi"><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover, interactive-widget=resizes-content"><meta name="theme-color" content="#020617"><title>TAPHOA STRUCTURE LOCK</title><link rel="preconnect" href="https://fonts.googleapis.com"><link rel="preconnect" href="https://fonts.gstatic.com" crossorigin><link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700;800&display=swap" rel="stylesheet"><style>${css}</style></head><body><div id="app-root"></div><div id="overlay-root"></div><script>${js}<\/script></body></html>`;
writeFileSync(resolve(root,'dist/TAPHOA_STRUCTURE_LOCK_STANDALONE.html'), html);
console.log('STANDALONE=PASS');
