name: One-shot geometry repair (retired)
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Composer geometry and narrow image viewer repair already applied."
