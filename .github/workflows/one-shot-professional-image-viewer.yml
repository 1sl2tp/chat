name: One-shot chat UI + mobile viewer polish (retired)
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Chat UI and mobile viewer polish already applied; workflow retired."
