name: One-shot professional image viewer (retired)
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Image viewer redesign already applied; workflow retired."
